import { css } from 'styled-components';
import { Colors } from '../../constants/styles'

export default css`
  margin: 20px;
  border: 1px solid ${Colors.BLUE};
  border-radius: 8px;
  padding: 15px
`